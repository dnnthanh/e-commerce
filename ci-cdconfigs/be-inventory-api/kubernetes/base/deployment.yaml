apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-inventory-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-inventory-api
  template:
    metadata:
      labels:
        app: be-inventory-api
    spec:
      containers:
        - name: be-inventory-api
          image: marketplace/be-inventory-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-inventory-api-config
            - secretRef:
                name: be-inventory-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
