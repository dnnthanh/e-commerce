apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-review-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-review-api
  template:
    metadata:
      labels:
        app: be-review-api
    spec:
      containers:
        - name: be-review-api
          image: marketplace/be-review-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-review-api-config
            - secretRef:
                name: be-review-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
