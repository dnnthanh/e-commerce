apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-settlement-outbox
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-settlement-outbox
  template:
    metadata:
      labels:
        app: be-settlement-outbox
    spec:
      containers:
        - name: be-settlement-outbox
          image: marketplace/be-settlement-outbox:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-settlement-outbox-config
            - secretRef:
                name: be-settlement-outbox-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
