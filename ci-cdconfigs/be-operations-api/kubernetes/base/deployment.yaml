apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-operations-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-operations-api
  template:
    metadata:
      labels:
        app: be-operations-api
    spec:
      containers:
        - name: be-operations-api
          image: marketplace/be-operations-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-operations-api-config
            - secretRef:
                name: be-operations-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
