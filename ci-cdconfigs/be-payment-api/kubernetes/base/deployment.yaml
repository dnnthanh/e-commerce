apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-payment-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-payment-api
  template:
    metadata:
      labels:
        app: be-payment-api
    spec:
      containers:
        - name: be-payment-api
          image: marketplace/be-payment-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-payment-api-config
            - secretRef:
                name: be-payment-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
