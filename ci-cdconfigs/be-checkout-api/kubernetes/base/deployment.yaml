apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-checkout-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-checkout-api
  template:
    metadata:
      labels:
        app: be-checkout-api
    spec:
      containers:
        - name: be-checkout-api
          image: marketplace/be-checkout-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-checkout-api-config
            - secretRef:
                name: be-checkout-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
