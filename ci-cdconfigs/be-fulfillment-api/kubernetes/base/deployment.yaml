apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-fulfillment-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-fulfillment-api
  template:
    metadata:
      labels:
        app: be-fulfillment-api
    spec:
      containers:
        - name: be-fulfillment-api
          image: marketplace/be-fulfillment-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-fulfillment-api-config
            - secretRef:
                name: be-fulfillment-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
