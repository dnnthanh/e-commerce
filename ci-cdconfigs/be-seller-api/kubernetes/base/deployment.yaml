apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-seller-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-seller-api
  template:
    metadata:
      labels:
        app: be-seller-api
    spec:
      containers:
        - name: be-seller-api
          image: marketplace/be-seller-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-seller-api-config
            - secretRef:
                name: be-seller-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
