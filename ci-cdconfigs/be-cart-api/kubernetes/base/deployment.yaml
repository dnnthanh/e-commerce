apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-cart-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-cart-api
  template:
    metadata:
      labels:
        app: be-cart-api
    spec:
      containers:
        - name: be-cart-api
          image: marketplace/be-cart-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-cart-api-config
            - secretRef:
                name: be-cart-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
