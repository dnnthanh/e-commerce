apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-operations-outbox
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-operations-outbox
  template:
    metadata:
      labels:
        app: be-operations-outbox
    spec:
      containers:
        - name: be-operations-outbox
          image: marketplace/be-operations-outbox:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-operations-outbox-config
            - secretRef:
                name: be-operations-outbox-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
