apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-return-outbox
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-return-outbox
  template:
    metadata:
      labels:
        app: be-return-outbox
    spec:
      containers:
        - name: be-return-outbox
          image: marketplace/be-return-outbox:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-return-outbox-config
            - secretRef:
                name: be-return-outbox-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
