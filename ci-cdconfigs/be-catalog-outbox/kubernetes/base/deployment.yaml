apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-catalog-outbox
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-catalog-outbox
  template:
    metadata:
      labels:
        app: be-catalog-outbox
    spec:
      containers:
        - name: be-catalog-outbox
          image: marketplace/be-catalog-outbox:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-catalog-outbox-config
            - secretRef:
                name: be-catalog-outbox-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
