apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-audit-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-audit-worker
  template:
    metadata:
      labels:
        app: be-audit-worker
    spec:
      containers:
        - name: be-audit-worker
          image: marketplace/be-audit-worker:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-audit-worker-config
            - secretRef:
                name: be-audit-worker-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
