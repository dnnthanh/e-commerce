apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-comment-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-comment-api
  template:
    metadata:
      labels:
        app: be-comment-api
    spec:
      containers:
        - name: be-comment-api
          image: marketplace/be-comment-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-comment-api-config
            - secretRef:
                name: be-comment-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
