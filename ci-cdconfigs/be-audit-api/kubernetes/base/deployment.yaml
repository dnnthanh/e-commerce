apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-audit-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-audit-api
  template:
    metadata:
      labels:
        app: be-audit-api
    spec:
      containers:
        - name: be-audit-api
          image: marketplace/be-audit-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-audit-api-config
            - secretRef:
                name: be-audit-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
