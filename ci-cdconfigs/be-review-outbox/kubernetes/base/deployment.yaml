apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-review-outbox
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-review-outbox
  template:
    metadata:
      labels:
        app: be-review-outbox
    spec:
      containers:
        - name: be-review-outbox
          image: marketplace/be-review-outbox:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-review-outbox-config
            - secretRef:
                name: be-review-outbox-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
