apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-inventory-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-inventory-worker
  template:
    metadata:
      labels:
        app: be-inventory-worker
    spec:
      containers:
        - name: be-inventory-worker
          image: marketplace/be-inventory-worker:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-inventory-worker-config
            - secretRef:
                name: be-inventory-worker-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
