apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-inventory-outbox
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-inventory-outbox
  template:
    metadata:
      labels:
        app: be-inventory-outbox
    spec:
      containers:
        - name: be-inventory-outbox
          image: marketplace/be-inventory-outbox:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-inventory-outbox-config
            - secretRef:
                name: be-inventory-outbox-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
