apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-media-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-media-api
  template:
    metadata:
      labels:
        app: be-media-api
    spec:
      containers:
        - name: be-media-api
          image: marketplace/be-media-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-media-api-config
            - secretRef:
                name: be-media-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
