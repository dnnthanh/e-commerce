apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-inventory-job
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-inventory-job
  template:
    metadata:
      labels:
        app: be-inventory-job
    spec:
      containers:
        - name: be-inventory-job
          image: marketplace/be-inventory-job:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-inventory-job-config
            - secretRef:
                name: be-inventory-job-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
