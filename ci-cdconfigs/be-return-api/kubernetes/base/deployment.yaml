apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-return-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-return-api
  template:
    metadata:
      labels:
        app: be-return-api
    spec:
      containers:
        - name: be-return-api
          image: marketplace/be-return-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-return-api-config
            - secretRef:
                name: be-return-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
