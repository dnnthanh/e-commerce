apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-search-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-search-worker
  template:
    metadata:
      labels:
        app: be-search-worker
    spec:
      containers:
        - name: be-search-worker
          image: marketplace/be-search-worker:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-search-worker-config
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
