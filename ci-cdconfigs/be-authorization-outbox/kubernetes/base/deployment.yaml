apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-authorization-outbox
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-authorization-outbox
  template:
    metadata:
      labels:
        app: be-authorization-outbox
    spec:
      containers:
        - name: be-authorization-outbox
          image: marketplace/be-authorization-outbox:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-authorization-outbox-config
            - secretRef:
                name: be-authorization-outbox-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
