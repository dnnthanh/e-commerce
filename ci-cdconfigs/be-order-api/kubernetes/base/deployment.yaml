apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-order-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-order-api
  template:
    metadata:
      labels:
        app: be-order-api
    spec:
      containers:
        - name: be-order-api
          image: marketplace/be-order-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-order-api-config
            - secretRef:
                name: be-order-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
