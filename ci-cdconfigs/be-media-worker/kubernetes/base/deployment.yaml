apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-media-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-media-worker
  template:
    metadata:
      labels:
        app: be-media-worker
    spec:
      containers:
        - name: be-media-worker
          image: marketplace/be-media-worker:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-media-worker-config
            - secretRef:
                name: be-media-worker-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
