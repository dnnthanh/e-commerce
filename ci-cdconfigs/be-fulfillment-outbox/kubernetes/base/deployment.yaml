apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-fulfillment-outbox
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-fulfillment-outbox
  template:
    metadata:
      labels:
        app: be-fulfillment-outbox
    spec:
      containers:
        - name: be-fulfillment-outbox
          image: marketplace/be-fulfillment-outbox:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-fulfillment-outbox-config
            - secretRef:
                name: be-fulfillment-outbox-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
