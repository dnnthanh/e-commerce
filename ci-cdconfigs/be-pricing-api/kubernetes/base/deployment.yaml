apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-pricing-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-pricing-api
  template:
    metadata:
      labels:
        app: be-pricing-api
    spec:
      containers:
        - name: be-pricing-api
          image: marketplace/be-pricing-api:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-pricing-api-config
            - secretRef:
                name: be-pricing-api-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
