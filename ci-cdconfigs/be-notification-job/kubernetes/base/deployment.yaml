apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-notification-job
spec:
  replicas: 1
  selector:
    matchLabels:
      app: be-notification-job
  template:
    metadata:
      labels:
        app: be-notification-job
    spec:
      containers:
        - name: be-notification-job
          image: marketplace/be-notification-job:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: be-notification-job-config
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
